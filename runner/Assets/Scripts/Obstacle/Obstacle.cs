using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obstacle : MonoBehaviour
{
    public GameObject[] obstacles;

    private int speed = 8;

    // Start is called before the first frame update
    void Start()
    {
        //Cargar(true, true, true);
    }

    // Update is called once per frame
    void FixedUpdate()
    {

    }

    /// <summary>
    /// Genera obstaculos de manera aleatoria (Rojo = alto; Azul = medio; Magenta = Bajo; cyan = Horizontal dos espacios; Verde = carro)
    /// </summary>



}
